from setuptools import setup, find_packages

setup(
    name="TermGUI",
    version="0.1",
    packages=find_packages(),
    install_requires=[],
    description="A simple terminal GUI library for Python.",
    author="Brave",
    license="MIT"
)
